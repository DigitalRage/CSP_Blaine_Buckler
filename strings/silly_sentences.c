//Blaine Buckler, silly sentences c
#include <stdio.h>
//string variables for my user inputs (minimum of 3)

int main(void){
    
    printf("What is your name?")
    //introduce user to program. . .tell them what is heppening (print statement)

    // create user inputs (print statements with questions AND scanf to collect the info)(same as the nuber of variables)

    //insert variables into the sentence to show the user (print statement, only 1) example: "hello %s", name"
    return 0; 
}